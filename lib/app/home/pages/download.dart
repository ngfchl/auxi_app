import 'package:flutter/cupertino.dart';

class DownloadPage extends StatelessWidget {
  const DownloadPage({super.key, param});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('DownloadPage'),
    );
  }
}
