import 'package:flutter/cupertino.dart';

class TaskPage extends StatelessWidget {
  const TaskPage({super.key, param});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('TaskPage'),
    );
  }
}
